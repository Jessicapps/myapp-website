name: Send Push Notifications

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  send-reminders:
    runs-on: ubuntu-latest
    steps:
      - name: Send reminders
        run: |
          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.BASE44_SERVICE_ROLE_KEY }}" \
            https://kalori-964108c6.base44.app/functions/checkAndSendReminders
